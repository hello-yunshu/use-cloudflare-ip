# OpenClash IntendedMappingV1

The transformer preserves supported protocol fields and creates marked variants. Readback requires the expected variant marker/name, exact server IP, target domain in SNI or Host, and configured transport evidence before probe/apply. User nodes not carrying the managed marker are never guessed or overwritten. Backup CRUD is independently dispatchable and restores with a pre-restore backup.
